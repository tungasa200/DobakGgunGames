# 버그 수정 이력

> 운영 중 발견된 버그를 아래에 계속 추가. 버그 발견 시 원인·수정 내용·수정 파일을 기록할 것.

## 수칙

1. **원인 먼저 파악** — 코드를 읽고 실제 원인을 확인한 뒤 최소 범위 수정. 관련 없는 코드는 건드리지 않을 것
2. **버그별 독립 수정** — 버그마다 영향 범위가 다르므로 한 버그 수정이 다른 버그에 영향을 주지 않도록 할 것
3. **원본 동작 기준** — 수정 후 원본(`excel-games/`)과 동일하게 동작하거나, 원본에 없는 버그라면 올바른 UX로 수정할 것

---

## 0: 새로 발견된 버그 (해결 시 아래 항목에 추가 후 이 리스트에서 삭제)

---

## ✅ 24: 회원가입 요청 시 405 (Method Not Allowed)

**원인**: 프론트엔드(Vercel)에서 `/api/auth/signup`으로 POST 요청 시 Vercel의 SPA 리라이트 규칙(`"/(.*)" → /index.html`)이 해당 경로를 가로챔. Vercel은 정적 파일에 POST 요청이 오면 405를 반환함. `auth.ts`와 `user.ts`가 상대경로(`/api/...`)를 사용하고 있어서, `rankings.ts`와 달리 `VITE_API_URL`을 참조하지 않아 백엔드로 직접 요청이 가지 않았음.

**수정**:
- `auth.ts`, `user.ts`: DEV 환경은 빈 문자열(Vite proxy 사용), 프로덕션은 `VITE_API_URL`을 prefix로 사용 (`rankings.ts`와 동일한 패턴)
- `vercel.json`: `/api/**` 경로를 SPA 리라이트에서 제외하는 passthrough 규칙 추가

**수정 파일**: `frontend/src/api/auth.ts`, `frontend/src/api/user.ts`, `frontend/vercel.json`

---

## ✅ 22: 로그인 후 메인 페이지 렌더링 안 됨 (nickname 크래시)

**원인**: `NormalHeader.tsx`에서 로그인 후 유저 아바타 표시 시 `user.nickname[0]`을 접근하는데, `nickname`이 `undefined`인 경우 `TypeError: Cannot read properties of undefined (reading '0')` 발생. React 에러 바운더리가 없는 경우 페이지 전체가 blank가 됨. URL은 `/`로 변경되지만 화면이 렌더링되지 않음.

**수정**: `user.nickname[0]` → `user.nickname?.[0] ?? '?'` (optional chaining + 폴백)

**수정 파일**: `frontend/src/components/normal/NormalHeader.tsx`

---

## ✅ 23: Google OAuth 로그인 시 프로필 사진·닉네임 미저장

**원인**: `CustomOAuth2UserService`에서 Google OAuth 처리 시 `picture` 속성을 가져오지 않아 신규 가입 유저의 `profileImage`가 항상 `null`로 저장됨. 또한 기존 유저 재로그인 시 프로필 사진이 갱신되지 않음. `generateUniqueNickname()`에서 `base.length()`로 substring 했지만 특수문자 제거 후 빈 문자열이 될 경우 `base.length()`와 `stripped.length()`가 달라 예외 발생 가능.

**수정**:
- 신규 유저: `picture` 속성 읽어 `profileImage`에 저장
- 기존 유저 재로그인: `profileImage`를 OAuth 제공자 사진으로 업데이트
- `generateUniqueNickname()`: `base.length()` → `stripped.length()` 로 수정
- 카카오(`profile_image_url`), 네이버(`profile_image`) 프로필 사진 필드도 추가

**수정 파일**: `backend/.../service/CustomOAuth2UserService.java`

---

## ✅ 21: 사과게임 모바일 세로 모드 시작 즉시 크래시

**원인**: 서버(`AppleSessionService`)는 항상 `10×17` 보드를 반환하지만, 모바일 세로(portrait) 모드에서 `calcLayout()`이 `rows=17, cols=10`으로 레이아웃을 계산함. `draw()` 내 이중 루프가 `layout.rows=17` 기준으로 `apples[10]`~`apples[16]`에 접근하는데 이 인덱스의 배열이 `undefined`이므로 `TypeError: Cannot read properties of undefined (reading '0')` 발생 → ErrorBoundary에 잡혀 화면 전체 소실.

**수정**:
- `handleStart()` 내에서 `startWithBoard()` 호출 전 `res.board.length` / `res.board[0].length`로 실제 보드 크기를 읽어 `setLayout()`으로 동기화 — 서버 보드와 레이아웃 rows/cols 불일치 근본 해결.
- `draw()` 내 `apples[r][c]` 접근 전 `!apples[r]` 방어 체크 추가 (혹시 불일치가 남아있어도 크래시 방지).

**수정 파일**: `games/apple/AppleCanvas.tsx`

---

## ✅ 19: 솔리테어 iOS Safari 카드 크기 최솟값 고정

**원인**: `useEffect` 내 치수 계산에서 `wrapRef.current?.clientWidth ?? window.innerWidth` 사용.
iOS Safari는 컴포넌트 마운트 시 레이아웃이 아직 미확정 상태로 `clientWidth = 0`을 반환.
`??`(nullish coalescing)은 `null`/`undefined`만 폴백하므로 `0`은 그대로 사용됨.
결과적으로 `vw = -40` → `Math.max(-40, 200) = 200` → 카드 최솟값(`cw = 44`)으로 고정.

**수정**: `??` → `||` 로 변경해 `0`도 폴백 처리. `ResizeObserver` 추가로 레이아웃 확정 후 재계산.

**수정 파일**: `games/solitaire/CardBoard.tsx`

---

## ✅ 20: 솔리테어 iOS Safari 문양 이모지 렌더링 및 점선 테두리 불일치

**원인 1**: iOS Safari는 ♥(U+2665), ♦(U+2666) 등 유니코드 기호를 컬러 이모지로 렌더링.
카드 안의 문양이 크게 표시되고 레이아웃이 틀어짐.

**원인 2**: iOS Safari의 `border-style: dashed` 렌더링 방식이 Android Chrome과 달라
빈 칸 점선 테두리의 대시 길이·간격·굵기가 다르게 보임.

**수정 1**: `SUIT_HINTS` 배열과 카드 렌더링(`cardTl`, `cardCenter`, `cardBr`)에 `\uFE0E` (text variation selector-15) 추가 → 텍스트 렌더링 강제. `.card` CSS에 `font-variant-emoji: text` 추가(이중 방어).

**수정 2**: `.zone`, `.tableauCol`의 `border: dashed` 제거 후 `background-image: repeating-linear-gradient` 4방향으로 교체 → 브라우저 무관 동일한 점선 렌더링.

**수정 파일**: `games/solitaire/CardBoard.tsx`, `games/solitaire/CardBoard.module.css`

---

## ✅ 16: 솔리테어 타블로 비최상단 카드 더블클릭 시 파운데이션 이동

**원인**: `autoMoveToFoundation`에서 tableau zone일 때 `index`가 스택의 마지막 인덱스인지 검사하지 않음.
예: 8D-7C-6H 스택에서 7C(index=1)를 더블클릭하면 7C만 파운데이션으로 옮기고 6H가 8D 위에 남는 비정상 상태 발생.

**수정**: `zone === 'tableau' && index !== stack.length - 1` 조건이면 즉시 return.

**수정 파일**: `games/solitaire/useSolitaireGame.ts`

---

## ✅ 17: 지뢰찾기 일반모드 역대 1위 배너 미표시

**원인**: 일반 모드 랭킹 섹션에 alltime 배너 렌더링 코드가 없었음. 엑셀 모드 랭킹 시트에만 역대 1위 행이 있었음.

**수정**: 일반 모드 주간 랭킹 테이블 하단에 `alltimeBanner` UI 추가. 원본(`minesweeper/index.html`)의 `#alltime-banner` 스타일과 동일한 디자인 적용.

**수정 파일**: `games/minesweeper/MinesweeperBoard.tsx`, `games/minesweeper/MinesweeperBoard.module.css`

---

## ✅ 18: 지뢰찾기 게임오버 시 올바른 깃발 위치가 지뢰로 표시

**원인**: `revealAllMines()`가 `cell.isMine && !cell.isRevealed` 조건으로 모든 지뢰를 열어버림.
깃발(`mark: 'flag'`)이 꽂혀 있는 지뢰도 `isRevealed: true`로 설정되어 렌더 시 '💣'로 표시됨.

**수정**: `cell.mark !== 'flag'` 조건 추가 → 깃발 있는 지뢰는 열지 않아 '🚩'가 유지됨.

**수정 파일**: `games/minesweeper/useMinesweeperGame.ts`

---

## ✅ 14: 랭킹 등록 모달 재사용 시 이름·버튼 상태 잔존

**원인**: `handleSubmitRanking` 성공 시 `setModalOpen(false)`만 호출하고 `playerName`과 `submitState`를 초기화하지 않음.
다음 게임 클리어 후 모달이 재오픈되면 이전 이름이 남아 있고, `submitState`가 `'loading'`인 채로 버튼이 "등록 중..."으로 표시됨.

**수정**: 성공 시 `setModalOpen(false)` 직후 `setPlayerName('')` · `setSubmitState('idle')` 추가.

**수정 파일**: `games/apple/AppleCanvas.tsx`, `games/baseball/BaseballBoard.tsx`, `games/blockfall/BlockfallBoard.tsx`, `games/minesweeper/MinesweeperBoard.tsx`, `games/solitaire/CardBoard.tsx`

---

## ✅ 15: 엑셀모드 하단 플러스 버튼 클릭 시 새 게임 미시작

**원인**: `ExcelShell.tsx`의 플러스 버튼 `onClick`이 `setActiveSheet('game')`만 호출하고 게임별 새 게임 함수를 호출하지 않음.
게임 컴포넌트에서 새 게임 함수를 셸에 전달하는 인터페이스 자체가 없었음.

**수정**:
- `ExcelShellContext`에 `registerNewGame(cb)` / `triggerNewGame()` 추가. 게임 컴포넌트는 마운트 시 콜백을 등록하고, 플러스 버튼은 `triggerNewGame()`을 호출.
- 각 게임은 "항상 최신 클로저"를 보장하기 위해 `newGameFnRef.current = fn` 패턴 사용 후 `registerNewGame(() => newGameFnRef.current())` 등록.
- Apple → `handleStart()`, Baseball → `reset(level); setInput(''); setHint(''); initSession(level)`, Blockfall → `startGame()`, Minesweeper → `reset(level)`, Solitaire → `handleStartGame(drawMode)`

**수정 파일**: `components/excel/ExcelShellContext.tsx`, `components/excel/ExcelShell.tsx`, 5개 게임 보드 파일

---

## ✅ 1-1: 솔리테어 랭킹 NaN 표시

**원인**: 백엔드의 `getAlltimeBest` API가 기록 없을 때 `null` 대신 빈 객체 `{}`를 반환함.
프론트엔드에서 `!!rankings.alltime` 체크 시 `!!{}` = `true`가 되어 alltime 배너를 렌더링하지만,
`.time`, `.moves`, `.name`, `.createdAt`이 모두 `undefined`이므로 `formatTime(NaN)` → "NaN초" 출력.

**수정**: `rankings.alltime` 체크를 `(rankings.alltime as {id?:number})?.id`로 변경하여
실제 데이터가 있는 경우만 렌더링.

**수정 파일**: `games/solitaire/CardBoard.tsx`

---

## ✅ 1-2 & 1-3: 솔리테어 엑셀모드 랭킹탭/룰탭 왼쪽 치우침

**원인**: `.wrap`에 `align-items: center`가 있고, `.excelMode`에서 이를 오버라이드하지 않음.
`xSheetWrapper`가 뷰포트보다 넓을 때 중앙 정렬로 왼쪽이 잘림.

**수정**: `CardBoard.module.css`의 `.excelMode`에 `align-items: flex-start` 추가.

**수정 파일**: `games/solitaire/CardBoard.module.css`

---

## ✅ 2: 블록폴 엑셀모드 랭킹탭/룰탭 스타일 불일치

**원인**: 블록폴 엑셀모드의 랭킹/룰 시트가 일반 `rankSection` HTML 구조를 사용.
다른 게임들은 `xSheetWrapper` → `xColHeaderRow` → `xBodyArea` → `xRankGrid` 구조 사용.

**수정**: 원본 `blockfall/excel.html`의 `buildRankingGrid()` / `buildRulesSheet()` 구조에 맞춰
React의 `xSheetWrapper` 구조로 교체. 보라색 테마(`#f3e9ff`, `#7b2ff7`) 적용.

**수정 파일**: `games/blockfall/BlockfallBoard.tsx`, `games/blockfall/BlockfallBoard.module.css`

---

## ✅ 3: 사과게임 간헐적 전체 엘리먼트 소실

**원인 1**: React ErrorBoundary 없음 — 렌더 중 에러 발생 시 `#root`가 비워짐.
**원인 2**: `getPos()` 함수에서 `touchend` 이벤트 처리 시 `e.touches[0]`이 undefined
(touchend 시 `touches` 배열이 빈 상태). `changedTouches[0]` 사용으로 수정 필요.

**수정 1**: `GamePage.tsx`에 ErrorBoundary 추가 — 게임 컴포넌트 감싸기.
**수정 2**: `AppleCanvas.tsx`의 `getPos()`에서 `touches[0] ?? changedTouches[0]` 폴백 처리.

**수정 파일**: `pages/GamePage.tsx`, `games/apple/AppleCanvas.tsx`, `components/ErrorBoundary.tsx` (신규)

---

## ✅ 4: 사과게임·숫자야구 랭킹 로드 시 컴포넌트 크래시

**원인**: 백엔드 `/rankings/alltime` API가 기록 없을 때 `null` 대신 빈 객체 `{}`를 반환함.
프론트엔드에서 `!!rankings.alltime` 체크 시 `!!{}` = `true`가 되어 배너 렌더 시도,
`.score`(사과) / `.attempts`(야구)가 `undefined` → `TypeError` → 컴포넌트 전체 크래시.

**영향 범위**:
- `AppleCanvas.tsx` 일반 모드 배너: `!!rankings.alltime`
- `AppleCanvas.tsx` 엑셀 모드: `loadExcelRanking`에서 raw 응답을 그대로 `setExcelAlltime`
- `BaseballBoard.tsx` 일반 모드 배너: `!!rankings.alltime`
- `BaseballBoard.tsx` 엑셀 모드: 이미 `'id' in` 체크로 안전 처리 중 ✅
- `MinesweeperBoard.tsx`, `CardBoard.tsx`, `BlockfallBoard.tsx`: 이미 안전 처리 중 ✅

**수정**:
- `AppleCanvas.tsx` `loadRanking` / `loadExcelRanking`: API 응답에서 `'id' in` 체크로 정규화, 없으면 `null` 저장
- `BaseballBoard.tsx` 일반 모드: `!!rankings.alltime` → `!!(rankings.alltime as {id?:number})?.id`

**수정 파일**: `games/apple/AppleCanvas.tsx`, `games/baseball/BaseballBoard.tsx`

---

## ✅ 5: 숫자야구 게임오버 없음

**원인**: `useBaseballGame.ts`의 `guess()` 함수가 정답(3S) 체크만 하고 `MAX_ATTEMPTS` 초과 체크를 하지 않음.
`State`에 `gameOver` 필드가 없고 reducer에도 `GAME_OVER` 액션이 없어 최대 횟수를 채워도 게임이 종료되지 않았음.

**수정**:
- `State`에 `gameOver: boolean` 추가, reducer에 `GAME_OVER` 액션 추가
- `guess()`에서 `attempt >= MAX_ATTEMPTS[state.level]`이고 정답이 아닐 때 `GAME_OVER` dispatch
- `BaseballBoard.tsx` 입력 필드·확인 버튼에 `state.gameOver` 비활성화 조건 추가
- 게임오버 시 `"게임 오버! 정답은 XXXX 였습니다."` 힌트 표시

**수정 파일**: `games/baseball/useBaseballGame.ts`, `games/baseball/BaseballBoard.tsx`

---

## ✅ 6: 숫자야구 히스토리 테이블 빈 행 key 충돌

**원인**: 테이블 tbody 렌더링에서 데이터 행은 `key={row.attempt}`, 빈 행은 `key={i}`(0-based 인덱스)를 사용.
`row.attempt`와 `i`가 같은 숫자가 되는 경우(예: attempt=5, i=5) React key 충돌 발생.

**수정**: 빈 행 key를 `key={i}` → `` key={`empty-${rowNum}`} ``로 변경하여 데이터 행과 충돌 방지.

**수정 파일**: `games/baseball/BaseballBoard.tsx`

---

## ✅ 7: 숫자야구 정답 항상 `012` 고정

**원인**: `generateAnswer()`에서 `pool.sort(() => Math.random() - 0.5)` 방식 사용.
이 방식은 V8 엔진의 TimSort에서 비일관적 비교 함수를 전달하면 원래 순서(`[0,1,2,...]`)가 보존되는 편향이 발생함.

**수정**: Fisher-Yates 셔플로 교체.
```ts
for (let i = pool.length - 1; i > 0; i--) {
  const j = Math.floor(Math.random() * (i + 1));
  [pool[i], pool[j]] = [pool[j], pool[i]];
}
```

**수정 파일**: `games/baseball/useBaseballGame.ts`

---

## ✅ 8: 숫자야구 클리어 시간 17165469878초 출력

**원인**: `startTimeRef.current = 0`(초기값)인 상태에서 첫 번째 guess 시 GUESS + WIN dispatch가
React batching으로 같은 렌더에서 처리됨. `timerRunning: false→true→false`가 한 렌더에 묶여
`useEffect([timerRunning])`이 실행되지 않아 `startTimeRef`가 0으로 유지됨.
결과적으로 `elapsed = (Date.now() - 0) / 1000 ≈ Unix timestamp(초)` 값이 그대로 노출.

**수정**:
- `guess()` 함수 내에서 `startTimeRef.current === 0`이면 `Date.now()`로 직접 설정 (useEffect 의존 제거)
- `reset()` 호출 시 `startTimeRef.current = 0`으로 초기화

**수정 파일**: `games/baseball/useBaseballGame.ts`

---

## ✅ 9: 숫자야구 일반모드 게임 시작 전 히스토리 테이블 미표시

**변경 요청**: 일반모드에서도 엑셀모드처럼 게임 시작 전부터 히스토리 테이블(`maxAttempts`개 빈 행)이 미리 표시되도록 수정.

**원인(기존 동작)**: `state.history.length === 0 && !excel` 조건에서 placeholder 텍스트(`"입력 기록이 여기에 표시됩니다."`)를 표시하고 테이블을 렌더하지 않았음.

**수정**: 조건 분기 제거, 항상 `Array.from({ length: maxAttempts }, ...)` 로 렌더.

**수정 파일**: `games/baseball/BaseballBoard.tsx`

---

## ✅ 10: 솔리테어 플레이타임 버그 (타이머 미동작 / 0초 기록)

**원인**: `useSolitaireGame.ts`의 `SET_GAME` 리듀서가 카드 첫 선택(클릭) 시 `status: 'idle' → 'playing'`으로 변경함.
이후 실제 이동 시 `ensureTimer`가 `state.status === 'idle'` 조건을 검사하는데 이미 `'playing'`이므로
`START_TIMER`가 dispatch되지 않아 `timerRunning`이 `false`로 유지 → 타이머 미동작 → `elapsed = 0`.

**수정**: `ensureTimer`의 조건을 `state.status === 'idle'` → `!state.timerRunning && state.status !== 'won'`으로 변경.
`status` 대신 `timerRunning` 플래그를 직접 확인.

**수정 파일**: `games/solitaire/useSolitaireGame.ts`

---

## ✅ 11: 솔리테어 기록 등록 HTTP 400

**원인**: 버그 10(타이머 미동작)로 인해 `elapsed = 0` → 기록 등록 시 `time: 0` 전송.
백엔드 `validateScoreBounds`에서 `time < 1` 조건으로 BAD_REQUEST 반환.

**수정**: 버그 10 수정으로 자동 해결.

---

## ✅ 12: 블록폴 기록 등록 HTTP 400

**원인**: `BlockfallValidationService`의 점수 상한 계산이 `linesCleared × 800 × gameLevel × 3`으로만 산정됨.
소프트·하드 드롭 보너스(soft: +1/행, hard: +2/행)는 `linesCleared`와 무관하게 발생하므로
`linesCleared = 0`이어도 `score > 0`이 될 수 있음 → `score > maxScore(=0)` → BAD_REQUEST.

**수정**: `maxScore` 계산에 드롭 보너스 최대치 `MAX_DROP_BONUS = 400` (20행 × 10열 × 2점) 추가.
`maxScore = linesCleared × 800 × gameLevel × 3 + 400`

**수정 파일**: `backend/.../service/BlockfallValidationService.java`

---

## ✅ 13: 지뢰찾기 최초 클릭 딜레이 로딩 표시 없음

**원인**: 최초 클릭 시 백엔드에서 지뢰를 배치(`startMinesweeperSession`)하는 동안 응답 대기 중임에도 UI 피드백이 없어 화면이 멈춘 것처럼 보임.

**수정**: `firstClickLoading` 상태 추가. 프리셋 난이도의 서버 요청 시작 시 `true`, 완료(성공/실패) 시 `false` 설정.
`boardWrapper` 위에 반투명 오버레이("지뢰 배치 중...")를 표시.

**수정 파일**: `games/minesweeper/MinesweeperBoard.tsx`, `games/minesweeper/MinesweeperBoard.module.css`
